[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "typer>=0.12.3",
    "pandas>=2.2.2",
    "pyarrow>=17.0.0",
    "numpy>=1.26.0",
    "plotly>=5.24.1",
    "fastapi[standard]>=0.114.0",
    "uvicorn>=0.30.0",
]

[project.scripts]
aida = "aida.cli:run"
aida-server = "aida.server:create_app"
